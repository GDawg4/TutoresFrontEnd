//import * as types from '../types/classes'

/*
export const startFetchingClass = () => ({
    type: types.CLASSES_FETCH_STARTED,
});

export const completeFetchingClass = (entities, order) => ({
    type: types.CLASSES_FETCH_COMPLETED,
    payload: {
        entities,
        order,
    },
});
export const failFetchingClass = error => ({
    type: types.CLASSES_FETCH_FAILED,
    payload: {
        error,
    },
});

export const selectClass = (oneClass)=>({
    type:types.CLASS_SELECTED,
    payload: oneClass,
})

export const deSelectClass = () => ({
    type:types.CLASS_DESELECTED,
})*/
